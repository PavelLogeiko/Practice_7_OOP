// абстрактный класс калькулятора, устанавливает, что у калькулятора для действий
// будут использоваться две переменные (два числа)
public abstract class CalcModel implements MainModel {
    int x, y;
}